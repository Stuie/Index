package app.sonderful.index.data

class Scanner {
    fun scan() {
        //TODO Scan the filesystem looking for stuff to index

        //TODO Build a database of things that can be returned (name, path, icon, some deterministic ID)
    }
}